# arguments in python
-An arguments also known as paarmeter.
-It is the value passed to the function.
-Difference- parameter -> variable and argument-> value that is given to the function

# Types of Argument:
1. Positional Argument
2. Default Argument
3. Arbitrary Argument
4. Arbitrary keyword argument
5. Mixed argument

# Argument Packing
-Argumnets passed to the parameter and then it is packed as tuple, list and dictionary etc.
# Unpacking argument
- Returns the value of the tuple and dictionary to the main.
# Lambda function
-is a small anonymous function in python.
used in
- map(), filter(), reduce(),sorted()-descending order sorting
## Types of lambda fn.
1. Lambda with No parameter
2. Lambda with one parameter
3. Lambda with multiple parameter
4. Lambda with conditional expression
5. Nested Lambda

# OOPS-Python
classes and objects
1.Class
- is a blueprint for object
- datatype used to create instance.
- it contains method and data.

2.Object
- is an instance of class.

#Encapsulation
consider objects that has meaning
class->1. data and 2. methods/function
data-> data and data structures 
function->inbuilt and user defined fns
object is inside class.

# Pillars of OOPS
private(_), public and productive(__) variable
getters- fn which has only written value, no parameter
setter=updating by user
1. Encapsulation-binding of data and functions. eg- class
                - security
                - data privacy
                - getters and setters to access private data

2. Polymorphism
3. Inheritance-class derived from another class
    Types-1. 
4. Abstraction

# Constructor -will construct the object
-is a special method that is automatically called when object is created.
-initializes objects.
-refers to the class.
will only have parameters,written values
## Types
1. default constructor(non parameterised constructor)
2. parameterised constructor 

__ init__- initialized
 deconstructor-destroy
