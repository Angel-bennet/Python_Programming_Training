# OOPS 
Fn overriding-
# Polymorphism
-2 types->
1. Compile time->1. Method overloading   - fn with the same name inside the same class
                 2. operator overloading - same operator used for different purpose
                    
2. run time-> 1. Method overiding-same fn in different classes
              2. Duck typing-

# Sliding window two pointer
take in ascending order.
expand the window (right++)and check whether they are <=k.
if the condition fails, shrink the window-> sliding to the right side and check whether the condition is true.
 

 # substring/subsequence
  1. Substring -> substring-part of string
  2. subsequence= a part of string not in order

  # Sliding window two pointer with variable pointer
  # Rat in a maze-uses dfs algorithm and backtrack pattern
